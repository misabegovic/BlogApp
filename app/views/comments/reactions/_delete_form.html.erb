<%= form_for([ comment, user_reaction(comment, type) ], method: :delete, remote: true) do |form| %>    
	<button type="submit" class="btn btn-primary">
		<%= humanize_type(type) %> <span class="badge badge-light"><%= reactions_count comment, type %></span>
	</button>
<% end %>