<div class="card" style="width: 18rem;">
    <div class="card-body">
        <h5 class="card-title"><%= post.title %></h5>
        <p class="card-text">
          <%= post.description %>
        </p>       
        <%= link_to 'Open', post_path(post), :class => 'btn btn-primary' %>
    </div>
</div>